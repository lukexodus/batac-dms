# TASK LIST — Module: REC (Records)

Generated per `A1-AGENTS.md` §6 "Step 2 — Module passes."
Wave F — runs after DOCS (Wave D), WF and TRACK (Wave E) task lists are complete. Runs in parallel with NOTIF.

**Documents loaded for this pass, in order:**
1. `docs/pre-development/A1-AGENTS.md` — Section 2 Pass Types table, Section 6 Step 2 rules (routing instructions, read first per those rules)
2. `docs/requirements-gathering/consolidated-architecture-and-requirements-reference-iteration-3.md` Part 13 Phase 1 capability list, plus Part 2, Part 11.7, Part 4.15 — **not on the user-supplied load list, added because A1-AGENTS.md §6 Step 2 requires reading "the capability list for this module in consolidated ref §13 Phase 1" before the Pass Types table documents; see Document Conflicts Resolved below**
3. `docs/pre-development/A-project-planning/a1-skeleton.md` (v2.1)
4. `docs/pre-development/A-project-planning/a1-tasks/wf.md` (25 tasks + Module Summary)
5. `docs/pre-development/A-project-planning/a1-tasks/track.md` (9 tasks + Module Summary)
6. `docs/pre-development/C-database/c1-full-database-schema-ddl-v3.md` §records (Part 8, L1715–L1831) — also Part 2 (L168–L234), Part 5 (L747–L900, for retention_schedule_id context), Part 12 (L1981–L2092), consulted to resolve cross-module sequencing questions raised below
7. `docs/pre-development/E-api-design/e1-trpc-router-and-procedure-catalog.md` Module 7 (L1218–L1276)
8. `docs/pre-development/B-architecture-documents/b2-module-boundary-and-internal-api-contracts-v1.1.md` Module 6 (L782–L852), Cross-Module Reference (L1156–L1212)
9. `docs/pre-development/I-security-and-authorization/i1-abac-policy-specification.md` §9 (L1074–L1198)
10. `docs/pre-development/I-security-and-authorization/i2-role-permission-matrix.md` §3 (L112–L134), §10 (L241–L260), Conditional Notes 5/15/16/17 (L367, L387, L389, L391)

**Also read (not on the required list; consulted to resolve a real cross-module sequencing question — see Document Conflicts Resolved):** `docs/pre-development/A-project-planning/a1-tasks/docs.md` (retention_schedule_id handling in TASK-DOCS-001 and TASK-DOCS-007, and its own `[SPEC-GAP-DOCS-03]`).

**Sourcing legend:**
- Unmarked — taken directly from one of the loaded sources.
- `[Inference]` — reasoned synthesis not stated verbatim in any loaded document.
- `[SPEC GAP]` — something a loaded source requires but no loaded document specifies clearly enough to write a self-contained AI Prompt for. Left for human resolution per `A1-AGENTS.md` §8.
- `[CONFLICT]` — an apparent disagreement between two loaded sources, flagged rather than resolved by guessing, per `A1-AGENTS.md` §1.

---

## Table of Contents

- [L36–L139] TASK-REC-001 — Drizzle ORM schema, SQL migration, and table grants for the records.retention_schedules table.
- [L140–L223] TASK-REC-002 — Idempotent seed script inserting the two Phase 1 retention categories matching hardcoded placeholder UUIDs.
- [L224–L303] Module Summary — Overview of tasks, provisional COA/DILG retention limits, deferred Phase 2 capabilities, and resolved conflicts.

---

## TASK-REC-001

Phase:          1
Module:         REC
Title:          [MIGRATION] Create records schema retention_schedules table
Prerequisites:  [CROSS-MODULE REF: INFRA — DB initialization and schema creation task; CREATE SCHEMA records is a Part 2 DDL statement, owned by INFRA per the same pattern TASK-TRACK-001 already established for its own schema; exact TASK-INFRA-NNN not identifiable from TASK-WF/TASK-TRACK lists alone; resolve at integration pass]
Deliverables:
  - /packages/database/schema/records.schema.ts `[Corrected — 2026-07-20, path and filename superseded, not deleted from history: original text specified /packages/database/src/schema/records.ts]` — Drizzle ORM table definition for records.retention_schedules (the only records.* table in Phase 1 scope) using pgSchema('records') and pgTable; all columns, the chk_retention_period CHECK constraint, and the updated_at trigger represented; named export re-exported from /packages/database/schema/index.ts (the existing barrel file, which currently re-exports six other .schema.ts modules — see EDIT A's verified facts block for the full list — using the same `export * from './{module}.schema.js';` pattern; add the records line following that identical pattern). classification_rules, records, archive_entries, and dispositions tables are explicitly NOT created by this migration — see Module Summary for why.
  - /apps/server/src/database/migrations/{timestamp}_create_records_retention_schedules.sql — SQL migration generated by `pnpm db:generate`, then manually extended with GRANT statements from C1 Part 12 scoped to this one new table (batac_app: SELECT/INSERT/UPDATE; batac_readonly: SELECT). No RLS policy is added — see AI Prompt for why.
Acceptance Criteria:
  - [ ] `pnpm db:generate` produces a migration file that, when applied via `pnpm db:migrate`, creates records.retention_schedules with zero errors on a database that already has the documents schema applied
  - [ ] `SELECT table_name FROM information_schema.tables WHERE table_schema = 'records' ORDER BY table_name` returns exactly: retention_schedules
  - [ ] `SELECT conname FROM pg_constraint WHERE conrelid = 'records.retention_schedules'::regclass AND contype = 'c'` includes chk_retention_period
  - [ ] Inserting a row with is_permanent = false and retention_period_years = NULL fails the chk_retention_period CHECK
  - [ ] `SELECT grantee, privilege_type FROM information_schema.role_table_grants WHERE table_schema='records' AND table_name='retention_schedules' ORDER BY grantee, privilege_type` shows batac_app with INSERT/SELECT/UPDATE and batac_readonly with SELECT only
  - [ ] `pnpm typecheck` passes at the workspace root
  - [ ] `pnpm db:migrate` is idempotent — running twice on the same database does not fail
AI Prompt: |
  You are implementing the Drizzle ORM schema for the `records` PostgreSQL schema and
  generating the corresponding SQL migration for the Batac City LGU document-management
  platform. This is a deliberately narrow migration: Phase 1 reserves exactly one table
  in the `records` schema. The other four tables that belong to this schema in the full
  data model — `classification_rules`, `records`, `archive_entries`, `dispositions` — are
  Phase 2 and are not created by this task.

  ## Why only one table, and why no ALTER TABLE on documents.*
  The `records` schema itself already exists — `CREATE SCHEMA IF NOT EXISTS records` was
  issued once, up front, alongside all other Phase 1 schemas, by INFRA's initial database
  setup migration (C1 Part 2). This task only adds a table inside it.

  `documents.document_types.retention_schedule_id` and `documents.documents.retention_schedule_id`
  already exist — they were created as part of the `documents` schema's own migration, not
  added later by this task. Both carry a code comment marking them "logical FK →
  records.retention_schedules.id (cross-schema)": a plain UUID column, not an enforced
  PostgreSQL FOREIGN KEY. Cross-schema references are never enforced at the database level
  in this project — do not add a REFERENCES clause pointing out of the `records` schema or
  into it from another schema.

  ## Project-wide DDL conventions
  - Every table: `id UUID NOT NULL PRIMARY KEY DEFAULT gen_random_uuid()`
  - Every table: `city_id UUID NOT NULL DEFAULT '00000000-0000-4000-8000-000000000001'::uuid`
  - All temporal columns: TIMESTAMPTZ
  - Soft delete only: `deleted_at TIMESTAMPTZ NULL` and `deleted_by UUID NULL` on every
    table. No table may ever receive a SQL DELETE — DELETE is revoked at the PostgreSQL
    grant level.
  - No FOREIGN KEY constraints across schema boundaries. Cross-schema references are plain
    UUID columns with a comment: `-- logical FK -> <schema>.<table>.<column> (cross-schema)`
  - Drizzle: use `drizzle-orm/pg-core`, `pgSchema`, `pgTable`, `uuid`, `text`, `integer`,
    `boolean`, `timestamp` helpers. Schema file: `/packages/database/schema/records.schema.ts`
    `[Corrected — 2026-07-20: original text specified /packages/database/src/schema/records.ts,
    which does not match the actual repository path or the unanimous .schema.ts naming
    convention used by all six existing schema files — see EDIT A above]`.

  ## Table definition — implement exactly as shown (C1 Part 8)
  ```sql
  CREATE TABLE records.retention_schedules (
      id                     UUID        NOT NULL PRIMARY KEY DEFAULT gen_random_uuid(),
      city_id                UUID        NOT NULL DEFAULT '00000000-0000-4000-8000-000000000001'::uuid,
      name                   TEXT        NOT NULL,
      code                   TEXT        NOT NULL,
      retention_period_years INTEGER     NULL,
      is_permanent           BOOLEAN     NOT NULL DEFAULT false,
      disposition_rule       TEXT        NULL,
      legal_basis            TEXT        NULL,
      -- configured_by: logical FK -> iam.users.id (cross-schema); Platform Admin, Tier 2
      configured_by          UUID        NULL,
      created_at             TIMESTAMPTZ NOT NULL DEFAULT now(),
      updated_at             TIMESTAMPTZ NOT NULL DEFAULT now(),
      deleted_at             TIMESTAMPTZ NULL,
      deleted_by             UUID        NULL,
      CONSTRAINT chk_retention_period CHECK (is_permanent = true OR retention_period_years IS NOT NULL)
  );

  CREATE TRIGGER trg_retention_schedules_set_updated_at
      BEFORE UPDATE ON records.retention_schedules
      FOR EACH ROW EXECUTE FUNCTION public.fn_set_updated_at();
  ```

  ## Grants (append to the generated migration — C1 Part 12 pattern, scoped to this table)
  ```sql
  GRANT SELECT, INSERT, UPDATE ON records.retention_schedules TO batac_app;
  GRANT SELECT ON records.retention_schedules TO batac_readonly;
  ```

  ## Do not add an RLS policy
  C1 Part 12's Row-Level Security section defines policies only for `documents.documents`
  (office isolation, IT admin content block) and `iam.sessions` (own-session visibility).
  No policy is specified for any `records` table. This matches the same finding
  TASK-TRACK-001 already made for the `tracking` schema — leave `retention_schedules`
  without RLS, consistent with C1 as written, rather than inventing a policy C1 does not
  specify.

  Before submitting this PR, confirm each item:
  - [ ] `pnpm db:generate` produces a migration file that, when applied via `pnpm db:migrate`, creates records.retention_schedules with zero errors on a database that already has the documents schema applied
  - [ ] `SELECT table_name FROM information_schema.tables WHERE table_schema = 'records' ORDER BY table_name` returns exactly: retention_schedules
  - [ ] `SELECT conname FROM pg_constraint WHERE conrelid = 'records.retention_schedules'::regclass AND contype = 'c'` includes chk_retention_period
  - [ ] Inserting a row with is_permanent = false and retention_period_years = NULL fails the chk_retention_period CHECK
  - [ ] `SELECT grantee, privilege_type FROM information_schema.role_table_grants WHERE table_schema='records' AND table_name='retention_schedules' ORDER BY grantee, privilege_type` shows batac_app with INSERT/SELECT/UPDATE and batac_readonly with SELECT only
  - [ ] `pnpm typecheck` passes at the workspace root
  - [ ] `pnpm db:migrate` is idempotent — running twice on the same database does not fail
  A reviewer will verify each one independently.

---

## TASK-REC-002

Phase:          1
Module:         REC
Title:          Seed records.retention_schedules — two Phase 1 retention categories
Prerequisites:  [TASK-REC-001, TASK-DOCS-007]
Deliverables:
  - /apps/server/src/database/seeds/retention-schedules.seed.ts — idempotent seed script (upsert keyed on the fixed `id` literals described in the AI Prompt) that inserts exactly two rows into records.retention_schedules: 'retention_permanent' (is_permanent=true, retention_period_years=NULL) and 'retention_citizens_correspondence' (is_permanent=false, retention_period_years=15, stored as a named, commented constant — see AI Prompt); script must run AFTER TASK-REC-001's migration is applied.
Acceptance Criteria:
  - [ ] Running the seed twice produces exactly two rows (idempotent)
  - [ ] `SELECT code, is_permanent, retention_period_years FROM records.retention_schedules ORDER BY code` returns: retention_citizens_correspondence(false,15), retention_permanent(true,NULL)
  - [ ] The `id` values of both seeded rows are byte-for-byte identical to the placeholder UUID constants already hardcoded in TASK-DOCS-007's `document-types.seed.ts` for 'retention_permanent' and 'retention_citizens_correspondence' — read that file directly to obtain the exact literals; do not generate new UUIDs
  - [ ] After this seed runs: `SELECT dt.code FROM documents.document_types dt LEFT JOIN records.retention_schedules rs ON rs.id = dt.retention_schedule_id WHERE dt.is_active = true AND rs.id IS NULL` returns zero rows (confirms every active document_type's placeholder id now resolves to a real retention_schedules row — closes TASK-DOCS-007's own `[SPEC-GAP-DOCS-03]`)
  - [ ] `pnpm typecheck` passes at the workspace root
AI Prompt: |
  You are writing the retention_schedules seed script for the Batac City LGU
  document-management platform. This seed closes a gap an earlier task left open on
  purpose: TASK-DOCS-007 (the document_types seed) already ran and inserted eight
  `documents.document_types` rows. Six of them (SP_RESOLUTION, SP_ORDINANCE,
  SP_APPROPRIATION_ORDINANCE, CERTIFICATION_OF_URGENCY, TRANSMITTAL_LETTER, DESIGNATION)
  have `retention_schedule_id` set to a hardcoded placeholder UUID pinned in that task
  under the name 'retention_permanent'. The other two (CITIZEN_COMPLAINT,
  DOCUMENT_REQUEST_FORM) use a second placeholder pinned under the name
  'retention_citizens_correspondence'. TASK-DOCS-007's own note (`[SPEC-GAP-DOCS-03]`)
  says: "After TASK-REC-NNN (retention schedules seed) runs, update the document_types
  seed to replace placeholder UUIDs with the actual schedule UUIDs" — but the simpler,
  correct fix is to make an update unnecessary: if this seed inserts its two rows using
  the SAME `id` values TASK-DOCS-007 already committed to, the placeholders become valid
  the instant this migration runs. Do not edit `document-types.seed.ts`.

  **Before writing this file, open the already-implemented
  `/apps/server/src/database/seeds/document-types.seed.ts` (or the constants module it
  imports the two placeholder UUIDs from) and copy the exact literal values.** No pre-dev
  document spells out the literal UUID strings — TASK-DOCS-007's own spec only shows an
  illustrative partial example ('a1b2c3d4-...'), not a real value. The real values exist
  only in that task's implementation. Do not invent new UUIDs for these two rows. If
  TASK-DOCS-007 has not actually been implemented in code yet when this task is picked
  up, coordinate directly with whoever implements it rather than guessing — the two seed
  files must agree on identical literals or the CHECK-constraint-only, no-enforced-FK
  design (see TASK-REC-001) will silently leave document_types pointing at nothing.

  ## Two rows to insert
  | code | name | is_permanent | retention_period_years | legal_basis | disposition_rule | configured_by |
  |---|---|---|---|---|---|---|
  | retention_permanent | Permanent Retention | true | NULL | NULL — not specified in any loaded source | NULL — not specified in any loaded source | NULL — system-seeded, not Platform-Admin-configured |
  | retention_citizens_correspondence | Citizen Correspondence | false | 15 (provisional — see below) | NULL — not specified in any loaded source | NULL — not specified in any loaded source | NULL — system-seeded, not Platform-Admin-configured |

  Source: consolidated reference Part 11.7 retention defaults table — "SP Resolutions,
  Ordinances: Permanent [CONFIRMED]" and "Correspondence with citizens: 10-15 years."
  **The 10-15 figure is a range, explicitly marked "configurable; to be confirmed with
  COA/DILG"** in that same source table — no loaded document picks a single integer. This
  task uses 15 (the upper/more conservative bound — safer for a no-deletion records system
  to over-retain than under-retain) as a provisional value only. Store it as a single
  named constant at the top of the seed file, not inline:
  ```typescript
  // PROVISIONAL: consolidated ref Part 11.7 gives a 10-15 year range for citizen
  // correspondence, explicitly marked "configurable; to be confirmed with COA/DILG."
  // Update this single constant once the real figure is confirmed.
  const CITIZENS_CORRESPONDENCE_RETENTION_YEARS = 15;
  ```

  ## Idempotency pattern
  Match the upsert style already used by TASK-DOCS-007's seed:
  ```typescript
  await db.insert(retentionSchedules).values(row).onConflictDoUpdate({
    target: [retentionSchedules.id],
    set: {
      name: sql`excluded.name`,
      isPermanent: sql`excluded.is_permanent`,
      retentionPeriodYears: sql`excluded.retention_period_years`,
    },
  });
  ```

  Before submitting this PR, confirm each item:
  - [ ] Running the seed twice produces exactly two rows (idempotent)
  - [ ] `SELECT code, is_permanent, retention_period_years FROM records.retention_schedules ORDER BY code` returns: retention_citizens_correspondence(false,15), retention_permanent(true,NULL)
  - [ ] The `id` values of both seeded rows are byte-for-byte identical to the placeholder UUID constants already hardcoded in TASK-DOCS-007's `document-types.seed.ts` for 'retention_permanent' and 'retention_citizens_correspondence' — read that file directly to obtain the exact literals; do not generate new UUIDs
  - [ ] After this seed runs: `SELECT dt.code FROM documents.document_types dt LEFT JOIN records.retention_schedules rs ON rs.id = dt.retention_schedule_id WHERE dt.is_active = true AND rs.id IS NULL` returns zero rows (confirms every active document_type's placeholder id now resolves to a real retention_schedules row — closes TASK-DOCS-007's own `[SPEC-GAP-DOCS-03]`)
  - [ ] `pnpm typecheck` passes at the workspace root
  A reviewer will verify each one independently.

---

## Module Summary

```
Module Summary — REC
Total tasks: 2 (TASK-REC-001 – TASK-REC-002)
First executable task: TASK-REC-001 (only prerequisite is CROSS-MODULE REF: INFRA,
  resolvable at the Step 4 integration pass — no dependency on any WF or TRACK task
  materialized; see Document Conflicts Resolved, item 5)
Spec gaps:
  [SPEC GAP: retention_period_years for 'retention_citizens_correspondence' — consolidated
   reference Part 11.7 gives a 10-15 year range explicitly marked "configurable; to be
   confirmed with COA/DILG." TASK-REC-002 seeds 15 as a clearly-flagged provisional
   constant. Needs COA/DILG confirmation before this figure is final.]
  [SPEC GAP: legal_basis and disposition_rule for both Phase 1 retention categories — no
   loaded document specifies category-level text for either column. Both left NULL
   (the columns are nullable; nothing in Phase 1 reads them).]
  [SPEC GAP: the literal UUID values TASK-DOCS-007 pinned for its two placeholder
   retention_schedule_id constants are not given in full in any loaded pre-dev document
   (only an illustrative partial example, 'a1b2c3d4-...'). TASK-REC-002 instructs its
   implementing agent to read the real values directly from TASK-DOCS-007's
   implementation. Flagged here in case the two tasks are implemented out of order or by
   people who do not otherwise coordinate.]
Deferred capabilities:
  [DEFERRED — Phase 2: records.classification_rules, records.records, records.archive_entries,
   and records.dispositions tables, and all associated Drizzle schema / migration work.
   C1 §records' own docstring names only retention_schedules as a Phase 1 dependency;
   nothing in the Phase 1 code path (confirmed against B2 Module 6, I1 §9, and WF's own
   Module Summary Deferred Capabilities table) reads or writes the other four tables.]
  [DEFERRED — Phase 2: recordsRouter tRPC procedures (E1 Module 7) — records.getRetentionSchedule,
   records.applyRetentionSchedule, records.applyClassification, records.placeLegalHold,
   records.removeLegalHold, records.isUnderLegalHold. See Document Conflicts Resolved,
   item 1 — E1 frames these as a "Phase 1 Subset," but B2, I1, and the consolidated
   reference all place the whole Records module in Phase 2; this task list follows the
   3-source majority per A1-AGENTS.md §1/§8.]
  [DEFERRED — Phase 2: RecordsPublicAPI (B2 Module 6) — getClassificationForDocument(),
   isUnderLegalHold(), getRetentionSchedule(). B2's own doc-comments confirm Phase 1 does
   not call these — the ABAC engine reads classificationLevel from
   Documents.getDocumentById() directly instead (ADR-API-007, "Confirmed as stated").]
  [DEFERRED — Phase 2: ABAC policies I1 §9.1–9.8 in full — record:promote,
   record:apply_retention_schedule, record:apply_classification, record:archive,
   record:bulk_archive, record:place_legal_hold / record:remove_legal_hold,
   record:initiate_disposition, record:pii_erasure. I1 §9's own header states this
   directly: "Records Management is Phase 2; the policies below apply when RMS is
   activated."]
  [DEFERRED — Phase 2: retention_schedules Platform Administrator CRUD UI (I2 §3 "Create /
   edit retention schedules"). E1 itself excludes this from "this catalog's Phase 1
   emphasis" even under its own (disputed) Phase 1 framing for the read/write subset — so
   this one item is deferred under every reading of the E1-vs-B2/I1 conflict, not only the
   reading this task list follows.]
  [DEFERRED — Phase 2: workflow.completed event consumer that creates a records.records row
   and an initial archive_entries row (B2 Module 6 Events Consumed). Independently
   confirmed from the WF side — WF's own Module Summary Deferred Capabilities table lists
   "Records module retention trigger on workflow.instance.completed | Phase 2" — both
   modules agree without either having read the other's Module Summary.]
  [DEFERRED — Phase 2: RA 10173 PII erasure workflow (I1 §9.8, consolidated ref Part 11.7).]
```

---

### Document Conflicts Resolved at Generation Time

| # | Conflict | Sources | Resolution Applied |
|---|---|---|---|
| 1 | **Is any part of the Records module's application layer (procedures, mutations) in Phase 1, or is Phase 1 schema-reservation only?** | E1 Module 7 header frames `recordsRouter` as a "Phase 1 Subset" and lists `getClassificationForDocument`, `isUnderLegalHold`, `getRetentionSchedule` as reads "the Documents/Workflow modules' published APIs already depend on," plus three mutations (`applyRetentionSchedule`, `applyClassification`, `placeLegalHold`/`removeLegalHold`) with no B2 counterpart at all. B2 Module 6 states `**Phase:** 2` outright, tags all three read methods `[Ph2]` in its dependency diagram, and its own doc-comment says plainly: "Phase 1 note: Records module is delivered in Phase 2. In Phase 1, the ABAC engine uses the classificationLevel field from `Documents.getDocumentById()` instead." I1 §9's header states: "Records Management is Phase 2; the policies below apply when RMS is activated." The consolidated reference (Part 13) lists "Records Management module" only under the Phase 2 addition list, not Phase 1. | **Followed B2 + I1 + the consolidated reference (3 of 4 loaded sources) per `A1-AGENTS.md` §1's precedence rule and §8's explicit instruction to exclude Phase-2-assigned capabilities even when a pre-dev document describes them in detail.** None of E1's six `recordsRouter` procedures are generated as Phase 1 tasks. Flagged as a `[CONFLICT]` for human resolution rather than silently dropped — either E1 needs correcting or B2/I1 need updating to match E1's intent; this task list does not presume to know which. |
| 2 | **Does `documents.documents.retention_schedule_id` (`NOT NULL`, no default) block Phase 1 document creation before REC's Wave F migration runs?** | C1 §documents (Part 5, L890–891) defines the column `NOT NULL` with no default. WF is Wave E (runs before REC's Wave F) and its Module Summary is silent on this column entirely. | **Resolved by direct inspection of `docs.md` (not on this pass's required reading list, consulted specifically to check this).** `documents.document_types` and `documents.documents` are DOCS-owned tables (Wave D), not WF's — WF creates `workflow.instances` referencing an already-created document, it does not itself insert `documents.documents` rows. TASK-DOCS-007 already resolved the `NOT NULL` constraint by seeding two hardcoded placeholder UUIDs (since the column has no enforced FK, any non-null UUID satisfies the constraint) and logged its own `[SPEC-GAP-DOCS-03]` naming this exact gap and expecting a future "TASK-REC-NNN" to close it. TASK-REC-002 closes it by design (matching `id` literals) rather than by a follow-up edit to DOCS's seed file. |
| 3 | **Does REC's Phase 1 migration need to add the `retention_schedule_id` column itself (as the Skeleton's speculative task list assumed), or does it already exist?** | `a1-skeleton.md` §6 REC row lists as speculative task 2: "[MIGRATION] add the `retention_schedule_id` reservation column to the relevant `documents`-schema table per H2" — implying the column does not yet exist. | **C1 Part 5 shows the column already inline in the `documents.document_types` / `documents.documents` `CREATE TABLE` statements**, and `docs.md`'s own reproduction of that DDL (in its already-written TASK-DOCS-001) confirms DOCS already created both columns. The Skeleton's assumption did not hold — its own reading list for the Step 1 pass was only `document-list.md`, `tech-stack.md`, and consolidated ref §10.2/§13, and did not include C1 itself, so this is a refinement made possible by reading C1 directly for this pass, not a new contradiction. No ALTER TABLE task is generated. |
| 4 | **Does REC's Phase 1 migration need a baseline RLS policy stub (as the Skeleton's speculative task list assumed, citing "the C3 convention")?** | `a1-skeleton.md` §6 REC row lists as speculative task 3: RLS stubs "per the C3 convention." C3 (`c3-postgresql-rls-policy-specifications.md`) is not on this pass's Pass Types table reading list. | **C1 Part 12 (Roles, Grants, and Row-Level Security) — a document this pass does have — defines RLS policies only for `documents.documents` and `iam.sessions`; no policy is specified for any `records` table.** This matches the same finding TASK-TRACK-001 already made and documented for its own schema ("No RLS policies on any tracking table — C1 Part 12 defines no RLS for the tracking schema"). No RLS task is generated; C3 was not needed to resolve this. |
| 5 | **Does any Phase 1 REC task actually need to reference a TASK-WF-NNN or TASK-TRACK-NNN ID in its Prerequisites field, as the Wave F dependency in Section 2 implies?** | Section 2's Pass Types table lists REC's Wave F as depending on the TASK-WF and TASK-TRACK lists; `A1-AGENTS.md` §6 Step 2 requires loading them "so that TASK-WF and TASK-TRACK IDs can be referenced in Prerequisites fields." | **Checked directly: no.** `track.md` contains zero mentions of "retention" anywhere. `wf.md` contains exactly one — its own Module Summary Deferred Capabilities table lists "Records module retention trigger on `workflow.instance.completed`" as `Phase 2`, i.e., the one place WF and REC would connect is itself deferred past this round. Both prerequisite lists were loaded and checked as instructed; the wave dependency exists as a standing rule for the REC pass in general, and in this specific narrow Phase 1 scope it resolves to "checked, nothing to reference." |
| 6 | *(Minor, non-blocking)* Schema file path naming is inconsistent across already-generated modules. | `[Corrected — 2026-07-20, superseded, not deleted]` Original claim: `wf.md` TASK-WF-001 deliverable: `/packages/database/src/schema/workflow.schema.ts`. `docs.md`, `iam.md`, and `track.md` all use `/packages/database/src/schema/{module}.ts` (no `.schema` suffix). Original resolution: followed the 3-of-4 majority convention: `/packages/database/src/schema/records.ts`. | `[Corrected — 2026-07-20]` **The original claim was checked directly against the repository filesystem and does not hold.** All six existing schema files (`audit.schema.ts`, `documents.schema.ts`, `iam.schema.ts`, `organization.schema.ts`, `tracking.schema.ts`, `workflow.schema.ts`) use the `.schema.ts` suffix, unanimously — there is no no-suffix majority; `workflow.schema.ts` is not an outlier, it matches the established pattern. The path is also corrected: the real directory is `/packages/database/schema/` (no `/src/` segment). Going forward, any future records schema file should be named `records.schema.ts` at `/packages/database/schema/records.schema.ts` to match the unanimous existing convention. |

---

### Cross-Validation Log

1. **Both required prerequisite task lists loaded before any task was drafted**, per the user's explicit instruction and `A1-AGENTS.md` §6: `wf.md` (25 tasks + Module Summary) and `track.md` (9 tasks + Module Summary) were read in full before TASK-REC-001 was written. Neither contains a task ID this module's Phase 1 scope needs to cite (see Document Conflicts Resolved, item 5).
2. **`A1-AGENTS.md` §6 Step 2's consolidated-reference requirement was not in the user's numbered document list and was added explicitly** (see the loaded-documents note at the top of this file) — Part 13 Phase 1's capability list for the "Foundation" phase does not name "Records Management" or any records-adjacent capability; Part 13 Phase 2 names it directly. This is the primary evidence behind Document Conflicts Resolved item 1, cross-checked independently against B2 and I1 before this task list treated it as settled.
3. **DOCS's placeholder-UUID mechanism (TASK-DOCS-007) was read directly, not assumed**, specifically to resolve the `retention_schedule_id NOT NULL` sequencing question — `docs.md` is not on this pass's required reading list; it was consulted because the alternative (guessing how DOCS handled a `NOT NULL` cross-schema column with no enforced FK, two waves before REC runs) would have violated `A1-AGENTS.md` §1's "never resolve a conflict by guessing" rule.
4. **The exact retention category slugs and their document_type assignments were taken verbatim from `docs.md`'s TASK-DOCS-007 AI Prompt** (`retention_permanent` used by 6 of 8 Phase 1 document types; `retention_citizens_correspondence` used by 2), not re-derived independently, to guarantee TASK-REC-002 seeds exactly the two categories DOCS already committed to and no others.
5. **`i1-abac-policy-specification.md.bak` was not read** — confirmed via `A1-AGENTS.md` §9 rule 5's superseded-file list, which names this exact file; only the live `i1-abac-policy-specification.md` was used.
6. **C1's Part 12 grant statements were checked for the `records` schema specifically** (`GRANT USAGE ON SCHEMA ... records ...`, `GRANT SELECT, INSERT, UPDATE ON ALL TABLES IN SCHEMA records TO batac_app`) — confirmed these are schema-wide, at-the-time-of-execution grants (not `ALTER DEFAULT PRIVILEGES`), which is why TASK-REC-001 grants explicitly on the new table rather than assuming a blanket INFRA-time grant already covers a table that does not exist yet at INFRA's migration time.
